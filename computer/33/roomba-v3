while turtle.detect()==false do
  turtle.forward()
end
while true do 
  if turtle.detectDown() then
    if turtle.detect() then
      turtle.turnLeft()
      else
      turtle.forward()
      turtle.turnRight()
      end
    else
    turtle.down()
  end
end
